float x
float y

on Touch(ref x, ref y, TOUCH_STATE_ENDS, TOUCH_INDEX_1) {
    inspect(vec3(x, y, 0))
}

readonly float a = 1

on Button(BUTTON_TYPE_DIRECTION) {
    inspect(a)
}

vec3 pos
joystick(ref pos, JOYSTICK_TYPE_SCREEN)
inspect(pos)