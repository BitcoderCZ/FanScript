on Play() {
    vec3 near
    vec3 far
    screenToWorld(200, 100, ref near, ref far)
    inspect(near)
    inspect(far)

    float screenX
    float screenY
    worldToScreen(vec3(5, 5, 5), ref screenX, ref screenY)
    inspect(screenX)
    inspect(screenY)

    vec3 screenPos = worldToScreen(vec3(5, 5, 5))
    inspect(screenPos)
}