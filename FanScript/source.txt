readonly float a = 1
readonly float b = 2

on Button(BUTTON_TYPE_DIRECTION) {
    inspect(a)
}
on Button(BUTTON_TYPE_BUTTON) {
    inspect(b)
}

vec3 pos
joystick(ref pos, JOYSTICK_TYPE_SCREEN)
inspect(pos)