on Swipe(out vec3 dir) {
    inspect(dir)
}

object obj1
on Collision(obj1, out object obj2, out float impulse, out vec3 normal) {
    inspect(obj2)
}

on Loop(0, 10, out float index) {
    inspect(index)
}